[project]
name = "miip-rerank-handler"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "runpod>=1.7",
    "torch==2.6.0",
    "transformers==4.51.3",
    "accelerate==1.6.0",
    "huggingface_hub",
]
